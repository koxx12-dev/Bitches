package gq.noxiuam.bitches.object;import com.github.javafaker.Faker;import gq.noxiuam.bitches.Main;import gq.noxiuam.bitches.object.data.Gender;import gq.noxiuam.bitches.object.data.Race;import lombok.*;import java.util.Random;@Getter@Setter@ToString@AllArgsConstructor@NoArgsConstructor
public class Bitch{private String name;private int age;private Race race;private Gender gender;public void build(){this.setName(new Faker().name().firstName());this.setAge(Main.random.nextInt(30)18);this.setRace(Race.values()[Main.random.nextInt(Race.values().length)]);this.setGender(Gender.values()[Main.random.nextInt(Gender.values().length)]);}}
